We have paperless deployed in documents.lacny.me, token is in .env file. We have all the invoices scanned and saved there. Fill-ups get `fuel`  other costs have `car` tag. There is always `total_amount` custom field with the sum paid, we will have (to be specified) custom field for date_time of the invoice. Create a task and a plan. The paperless URL should be configurable, the PAT used should be configurable as well (just like we have homeassistant) - with checking if it's live and working.

If paperless is setup - the "doklady" page would NOT use local invoices rather the paperless ones. Will not offer "znovu spracovat" button, nor "edit" button, "open" would open in paperless link, remove would not be visible. Assignment and all else would work as usual, but the source of truth for the invoices would be paperless.
