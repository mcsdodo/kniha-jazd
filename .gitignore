node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*


# Worktrees
.worktrees/

# Rust build artifacts (rust-analyzer workspace cache)
/target/

# Staged SPA (copied from build/ by scripts/stage-spa.mjs at bundle time).
# .gitkeep is tracked so the bundle.resources glob always matches during cargo build.

kniha_jazd.xlsx

# Generated databases
scripts/*.db
# Production snapshots pulled into a task folder for analysis. This repository is
# public and the logbook holds real home and business addresses, so no database
# reaches it by accident.
_tasks/**/*.db
.claude/settings.local.json*
.claude/scheduled_tasks.lock
scripts/compare_import.py
scripts/import_excel.py

# Integration test screenshots
tests/integration/screenshots/

local.settings.json
tmpclaude-*


analyze-routes.mjs
cleanup-routes.mjs
_tmp

# Docker integration-test work dir (bind-mounted to /data in the container).
# Anchored to the repo root: an unanchored "data/" also swallows
# tests/integration/data/, silently untracking new fixtures.
/data/
/data-*/
CLAUDE.local.md